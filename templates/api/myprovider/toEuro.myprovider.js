/**
 * Template function to convert a currency to the Euro.
 * @param currency Symbol to convert to the Euro
 * @return {Promise<number>} ratio to the Euro or 1 in case of errors
 */
// eslint-disable-next-line no-unused-vars
export default async function(currency) {
    return 1;
}